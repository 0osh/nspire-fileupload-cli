
# nspire-fileupload-cli

Very simple CLI for uploading files onto a TI-nspire calculator. Puts files in `/documents/` (top level of the list when you click Browse)

### Build
```
go build
```

### Usage
```
./nspire-fileupload-cli path/to/file.tns
```
